FROM node:22-alpine AS build
WORKDIR /app
RUN corepack enable
COPY . .
RUN pnpm install --frozen-lockfile && pnpm --filter @ciso-grc/api build
EXPOSE 3001
CMD ["pnpm", "--filter", "@ciso-grc/api", "start"]
