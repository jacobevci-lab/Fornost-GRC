import { NextRequest,NextResponse } from "next/server";
import { requireRole } from "../auth/security";
const table=`CREATE TABLE IF NOT EXISTS simple_grc_records (id TEXT PRIMARY KEY,module TEXT NOT NULL,data_json TEXT NOT NULL,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`;
export async function POST(req:NextRequest){const auth=await requireRole(req,["Admin","Editor"]);if(auth.response)return auth.response;const length=Number(req.headers.get("content-length")||0);if(length>11*1024*1024)return NextResponse.json({error:"İstek boyutu çok büyük."},{status:413});const {env}=await import("cloudflare:workers");if(!env.BUCKET)return NextResponse.json({error:"Dosya alanı hazır değil"},{status:503});await env.DB.prepare(table).run();const fd=await req.formData(),file=fd.get("file");if(!(file instanceof File))return NextResponse.json({error:"Dosya gerekli"},{status:400});if(file.size<1||file.size>10*1024*1024||!["application/pdf","image/png","image/jpeg","image/webp"].includes(file.type))return NextResponse.json({error:"Yalnız PDF/JPG/PNG/WebP ve en fazla 10 MB dosya yüklenebilir."},{status:400});const bytes=new Uint8Array(await file.arrayBuffer()),head=[...bytes.slice(0,12)],valid=file.type==="application/pdf"?String.fromCharCode(...head.slice(0,5))==="%PDF-":file.type==="image/png"?head.slice(0,8).join(",")==="137,80,78,71,13,10,26,10":file.type==="image/jpeg"?head[0]===255&&head[1]===216&&head[2]===255:file.type==="image/webp"?String.fromCharCode(...head.slice(0,4))==="RIFF"&&String.fromCharCode(...head.slice(8,12))==="WEBP":false;if(!valid)return NextResponse.json({error:"Dosya içeriği bildirilen türle eşleşmiyor."},{status:400});const required=["evidenceTitle","controlRef","owner","period"];for(const field of required)if(!String(fd.get(field)||"").trim())return NextResponse.json({error:`Zorunlu alan eksik: ${field}`},{status:400});const id=`EVD-${crypto.randomUUID()}`,safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,"_").slice(-120)||"evidence",key=`evidence/${id}/${safeName}`;await env.BUCKET.put(key,bytes,{httpMetadata:{contentType:file.type}});const data:Record<string,string>={fileKey:key,fileName:safeName,fileType:file.type};for(const [k,v] of fd.entries())if(k!=="file"&&typeof v==="string")data[k]=v.trim().slice(0,2000);const now=new Date().toISOString();await env.DB.prepare("INSERT INTO simple_grc_records(id,module,data_json,created_at,updated_at) VALUES(?,?,?,?,?)").bind(id,"Kanıtlar",JSON.stringify(data),now,now).run();return NextResponse.json({ok:true,id},{status:201})}
export async function GET(req:NextRequest){const auth=await requireRole(req,["Admin","Editor","Viewer"]);if(auth.response)return auth.response;const {env}=await import("cloudflare:workers"),key=req.nextUrl.searchParams.get("key");if(!key||!key.startsWith("evidence/")||!env.BUCKET)return new NextResponse("Bulunamadı",{status:404});const obj=await env.BUCKET.get(key);if(!obj)return new NextResponse("Bulunamadı",{status:404});return new NextResponse(obj.body,{headers:{"content-type":obj.httpMetadata?.contentType||"application/octet-stream","content-disposition":`attachment; filename=\"${key.split('/').pop()}\"`,"x-content-type-options":"nosniff","cache-control":"private, no-store"}})}
